
namespace Assets.Scripts.GameManager.GameEvents.State
{
    public enum MatchState
    {
        preMatch,
        warming,
        starting,
        playing,
        pause,
        gameOver,
        exit,
    }
}
